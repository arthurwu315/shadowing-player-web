# YouTube 跟讀播放器（GitHub Pages 版）

這是一個單檔 `index.html` 的 YouTube 跟讀工具，目標是「免伺服器、可直接放 GitHub Pages、手機可用」。

## 目前功能

- 深色模式介面（Tailwind CSS）。
- 貼上 YouTube 網址後自動載入影片。
- 透過免金鑰字幕 API 嘗試抓字幕（先 Supadata，再備援來源）。
- API 失敗時會顯示手動流程提示：`自動抓取受限，請點擊下方按鈕手動貼入英/中雙語講稿以開始練習`。
- 手動貼上雙語字幕：支援「英文講稿」與「中文翻譯」兩欄同時貼上。
- 智慧格式解析與對齊：
  - 格式 A（帶時間）：`00:12 Hello world`
  - 格式 B（時間與文字分行）：如 `0:03` 下一行是句子內容。
  - 格式 C（純文字）：每行一句，依「每句秒數」自動分配時間軸。
  - 兩邊都有時間戳記時，會自動按時間配對。
  - 純文字時，會按行數對齊。
  - 單邊有時間、另一邊純文字時，會對齊到有時間的一側。
- 列出所有字幕行。
- 點擊任一字幕行可跳到該句開始時間。
- 每行都有 `🔁 循環` 按鈕，可單句重複播放。
- Auto-Pause 自動留白模式：句尾自動暫停 `該句時長 x 1.2`，再跳到下一句。
- 快捷大按鈕：`上一句`、`重新播放本句`、`下一句`。
- 播放速度快捷切換：`0.75x`、`1x`、`1.25x`。
- 計數器：每句可累積練習次數（點擊跳句 +1、句子播完 +1），並儲存到 `localStorage`。
- 盲聽模式：預設字幕模糊，滑鼠懸停或手指按住時才清晰。
- 句子級顯示控制：每句字幕卡片都有 `英` / `中` 按鈕可獨立顯示/隱藏。
- 句子級播放控制：每句字幕卡片新增 `⏯ 播放/暫停`，可就地中斷或續播練習。
- 單句播放鍵即時狀態：按鈕會依播放器狀態切換 `▶️ / ⏸️`。
- 單句獨立調速：每句卡片可直接用微型按鈕切換語速（慢速 / 1x）並立即跳轉該句。
- 句間自動恢復語速：可切換「跳下一句回 1x」，預設開啟避免忘記回到正常語速。
- 全局顯示控制面板：
  - 全部顯示英文
  - 全部顯示中文
  - 全部顯示雙語
  - 全部模糊（盲聽）
  - 僅顯示當前句子（Focus Mode）
- 全域懸浮控制塔：播放器與全局控制面板整合為頂部固定的毛玻璃控制區。
- 藥丸按鈕 UI：全局控制改為圖示 + 文字的 pill 風格，點擊辨識更直覺。
- Lo-Fi Pixel 視覺規範：暖米色底、2px 黑邊、硬陰影與莫蘭迪按鈕配色。
- 邏輯解耦：Focus Mode 與 Blind Mode 可獨立開啟，不互相觸發。
- 經典跟讀片單：內建 3 個影片卡片，點擊可自動填入影片 ID。
- 手機友善：按鈕高度加大、間距加大，方便點擊。
- 記憶優化：同影片 ID 會記住雙語內容、每句英中顯示狀態、全局顯示設定與計數器（`localStorage`）。
- 當前句自動顯現：即使在單句循環或全局模糊中，播放器當前句會自動清晰顯示，保持跟播放進度同步。
- 調速穩定性：切換 `0.75x / 1x / 1.25x` 不會讓當前句消失或重新被模糊。
- 自動置中捲動：進入下一句時，會平滑捲動把當前句置中，手機上更好跟讀。
- 高頻同步：時間偵測提升至每 100ms 一次，降低切句與圖示反應延遲。
- 當前句視覺焦點：亮藍邊框 + 陰影 + 微放大，提升跟讀專注感。
- 計數器成就標籤：每句練習次數改為圓形徽章樣式。
- 橫向模式優化：手機 Landscape 會壓縮控制塔高度，保留更大影片區並維持至少一行字幕可見。

## 檔案結構

- `index.html`：全部前端邏輯（UI、YouTube 播放器、字幕抓取、跳句、單句循環、Auto-Pause、快捷控制、速度切換、計數器、盲聽模式）。
- `README.md`：專案說明與維護紀錄。

## 如何在本機使用

1. 直接用瀏覽器開啟 `index.html`。
2. 貼上 YouTube 網址（例如 `https://www.youtube.com/watch?v=...`）。
3. 按「載入影片與字幕」。
4. 看到字幕列表後：
   - 點「跳到這句」可跳轉播放。
   - 點 `🔁 循環` 可重播該句，點「停止循環」可解除。
   - 開啟 Auto-Pause 可自動句尾留白並接續下一句。
   - 使用 `上一句 / 重播 / 下一句` 快速操控。
   - 每句右側會顯示累積練習次數。
5. 若自動抓字幕失敗：
   - 點「手動貼上字幕」。
   - 貼上 `00:12 句子` 或每行純文字。
   - 按「套用手動字幕」即可練習。

## 如何免費部署到 GitHub Pages（手機可直接連）

1. 在 GitHub 建立新 repository（例如 `youtube-shadowing-player`）。
2. 把目前專案檔案上傳（至少 `index.html`、`README.md`）。
3. 到 repository 的 **Settings** → **Pages**。
4. **Build and deployment** 選：
   - Source: `Deploy from a branch`
   - Branch: `main`（或你的分支）/ root
5. 儲存後等待 1~3 分鐘，會出現網址：
   - `https://<你的帳號>.github.io/<repo名稱>/`
6. 用手機開啟該網址即可使用。

## 注意事項（重要）

- 由於字幕 API 是第三方服務，可能受限於：
  - CORS 規則
  - 配額限制
  - 回傳格式變動
- 如果某天無法抓字幕，不代表網頁壞掉，可能是 API 服務策略改變。可替換 `index.html` 內 `providers` 陣列中的來源。

## 維護規則（每次修改都要更新）

之後每一次功能修改，請同步更新本檔：

- `目前功能`：反映實際功能狀態。
- `檔案結構`：若新增檔案要補上。
- `注意事項`：記錄新的相依風險或限制。
- `更新紀錄`：新增一筆變更說明。

## 更新紀錄

- 2026-02-22（Lo-Fi Pixel 風格轉型）
  - 導入暖米色基底與像素化元件語言（黑邊 + 硬陰影），建立一致品牌視覺。
  - 全按鈕改為莫蘭迪配色並保留按壓縮放/位移觸感回饋。
  - 標題與計數器採用 VT323 像素字體，字幕內容維持高可讀深炭灰字色。
  - 當前句卡片改為淡黃底 + 亮藍邊框 + 陰影 + scale，焦點更明確。
  - 強化懸浮控制塔高度補償與置中邏輯，並加入 Landscape 版面優化。
- 2026-02-22（UIUX 控制塔大改版）
  - 播放器與全局控制整合為「全域懸浮控制塔」，加入毛玻璃背景、分隔線與陰影。
  - 調整字幕區偏移策略，避免首句被頂部懸浮區遮擋。
  - 重新校準當前句置中邏輯，考慮懸浮面板高度進行平滑定位。
  - 全局控制按鈕改為圖示化藥丸風格，並保留完整狀態同步。
  - 當前句高亮升級為亮藍邊框 + 陰影 + scale 微動畫，辨識更明顯。
  - 卡片計數器改為圓形成就標籤，整體視覺更一致。
- 2026-02-22（邏輯與置中修復）
  - 修復 Focus/Blind 耦合問題，兩個模式可分別獨立運作。
  - 聚焦模式改為僅保留當前句顯示，非當前句完全隱藏。
  - 置中邏輯統一使用 `scrollIntoView({ behavior: 'smooth', block: 'center' })`。
  - 同步輪詢提升至 100ms，改善卡片切換、置中與 `▶️ / ⏸️` 即時性。
  - 當前句高亮改為亮藍 3px 邊框與陰影，視覺辨識更明確。
- 2026-02-22（單句獨立控制與視覺體感優化）
  - 每句卡片新增獨立語速切換（慢速 / 1x），可直接跳轉該句並套用語速。
  - 新增「跳下一句回 1x」開關，預設開啟，並支援 `localStorage` 記憶。
  - Focus Mode 改為非當前句 `display: none` 隱藏，強化單句專注。
  - 進入下一句時自動平滑置中捲動，改善手機練習體感。
  - 全按鈕加入點擊縮放動效，當前句加強亮藍邊框與陰影高亮。
  - 播放圖示同步改進：以播放器狀態事件 + 輪詢維持 `▶️ / ⏸️` 一致。
- 2026-02-22（功能回歸與補強）
  - 回歸 Focus Mode：新增「僅顯示當前句子」總開關，可在字幕清單與單句聚焦間切換。
  - 單句卡片播放/暫停按鈕升級為即時圖示（`▶️ / ⏸️`）並與播放器狀態同步。
  - 修復調整語速時當前句顯示穩定性，確保正在播放句維持可見與清晰。
  - 確保 Focus Mode 與雙語顯示邏輯相容，並將 Focus 開關納入 `localStorage`。
  - 保持輕量前端輪詢與定時器清理，避免累積型資源佔用風險。
- 2026-02-22（最終完美版修復）
  - 修復當前句顯現邏輯：不論一般播放或單句循環，當前句會自動解除模糊。
  - 修復狀態同步問題：調速或操作全局控制時，正在播放句仍保持可見並跟隨進度。
  - 每句字幕卡片新增 `⏯ 播放/暫停` 控制，免回到上方主控列即可操作。
  - 全局控制升級為 4 個總開關（英文/中文/雙語/盲聽）並持久化到 `localStorage`。
  - 維持雙語輸入、時間戳/行數對齊與單句英中顯示控制。
- 2026-02-22（雙語控制升級 + 桌面工作副本）
  - 新增雙語輸入區（英文講稿 / 中文翻譯）與雙語自動對齊邏輯。
  - 新增每句 `英` / `中` 顯示切換按鈕，支援句子級顯示控制。
  - 新增全局控制面板（全顯/隱英文、全顯/隱中文、全局盲聽模式）。
  - 新增雙語資料與顯示狀態記憶（按影片 ID 回復）。
  - API 失敗提示文字更新為雙語手動貼入引導訊息。
  - 為避開 OneDrive 影響，改在桌面獨立副本維護與提交。
- 2026-02-22（進階跟讀版）
  - 新增 Auto-Pause 句尾留白（`duration x 1.2`）與自動跳下一句。
  - 新增三個快捷大按鈕（上一句、重播本句、下一句）。
  - 新增播放速度快速切換（0.75x / 1x / 1.25x）。
  - 新增每句計數器與 `localStorage` 持久化。
  - 新增盲聽模式（預設模糊，懸停/按住才清晰）。
  - 重整 `index.html` 成單一乾淨版本，避免重複內容，確保功能集中在單檔可直接部署。
- 2026-02-22（手動字幕修復版）
  - 新增「手動貼上字幕」彈窗，讓 API 失效時可直接貼字幕內容使用。
  - 新增智慧解析：支援帶時間與純文字兩種輸入格式。
  - API 失敗訊息調整為可行動提示，引導使用者改走手動貼上。
  - 確保手動字幕沿用 Auto-Pause、循環、快捷按鈕、播放速度與計數器功能。
  - 新增「經典跟讀片單」3 張影片卡片，點擊可自動填入 ID。
- 2026-02-22（播放穩定與中文介面版）
  - 介面文字調整回中文顯示。
  - 強化手動字幕解析，支援 YouTube 常見的「時間行 + 下一行字幕」格式。
  - 調整前兩個預設片單為較穩定可播放的影片 ID。
- 2026-02-22
  - 建立首版：單檔 `index.html` 的跟讀播放器。
  - 完成字幕抓取、點擊跳句、單句循環、手機友善按鈕。
  - 補上 GitHub Pages 上線教學與維護規則。

